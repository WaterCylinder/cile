测试版回合逻辑

1. 开始游戏前，玩家选择角色（或许有选择技能之类的）
2. 第一回合开始前，玩家决定先后手
3. 第一回合到第四回合，四个玩家依次行动，选择一个准备区然后放置一个大单位一个小单位
4. 第五回合开始回合循环，当前玩家进行操作：
    回合内玩家可以执行的操作以及资源分配：
    点击单位进行操作
    点击下一回合按钮
5. 

单位逻辑
单位有属性和行为，属性属于可扩展内容
行为分为三个类型：
1. 主动行为，主动行为可以在选择单位后点击行为按钮触发
主动行为的触发逻辑如下：
    1. 玩家选择包含单位的地形，在属性栏里切换到单位选择
    2. 在单位选择的UI里点击行为按钮，触发行为开始操作
    3.  举个例子，移动行为，当玩家点击移动行为按钮时，显示单位的可移动范围，那之后会切换游戏状态到地形选择状态
        地形选择状态下玩家选择地形并点击确认，这时开始执行单位行为
    4. 单位行为代码层面执行完毕，开始播放动画。
2. 被动触发行为：
    单位被放置到场上时，将被动触发行为添加到指定的signal里

单位战斗：
单位选择战斗相关效果后，显示战斗范围，点击范围内的非当前玩家的单位触发战斗。
触发战斗后弹出战斗UI，可以在战斗UI里选择单位操作，每个单位有1-4个操作
    